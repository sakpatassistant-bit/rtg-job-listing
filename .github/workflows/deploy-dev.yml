name: Deploy to Dev

on:
  push:
    branches: [dev]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: development
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build \
            --build-arg API_URL=https://erp-dev.reanthai.com \
            --build-arg NEXT_PUBLIC_API_URL=https://erp-dev.reanthai.com \
            -t rtg-job-listing:dev .

      - name: Save Docker image
        run: docker save rtg-job-listing:dev | gzip > rtg-job-listing-dev.tar.gz

      - name: Copy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "rtg-job-listing-dev.tar.gz,docker-compose.dev.yml"
          target: "/opt/rtg-job-listing-dev"

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /opt/rtg-job-listing-dev
            gunzip -c rtg-job-listing-dev.tar.gz | docker load
            docker compose -f docker-compose.dev.yml down || true
            docker compose -f docker-compose.dev.yml up -d
            rm rtg-job-listing-dev.tar.gz
            docker system prune -f
